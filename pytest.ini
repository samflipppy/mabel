[pytest]
testpaths = tests
asyncio_mode = auto
# Session-scoped isolation fixtures (the scratch Postgres engine) need a
# session loop. pytest-asyncio 1.x defaults fixtures to function scope,
# which makes the whole RLS suite error at setup.
asyncio_default_fixture_loop_scope = session
